//query selector= getElementById + getElementsByClassNam 

//this means that query selctors can do all the work which we can do by both of these query

document.querySelectorAll(".name");
// this will select all the Element with class=name

//while queryselector select only first element if class passed through it is applied to multiple element